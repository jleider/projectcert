name: Audit ledger sync (nightly)

# Exports the review console's checkmark ledger from D1 into the repo so
# the public per-state pages can show a "datapoints reviewed" badge. This
# workflow NEVER edits state JSON or verificationStatus — it only writes
# src/data/verification-ledger.json and opens a PR when it changes.

on:
  schedule:
    - cron: "0 6 * * *" # daily 06:00 UTC
  workflow_dispatch:

permissions:
  contents: write
  pull-requests: write

jobs:
  sync-ledger:
    name: Export D1 ledger
    runs-on: ubuntu-latest
    env:
      CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
      CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
    steps:
      - uses: actions/checkout@v7

      # Every step below is gated on the Cloudflare credentials being
      # present. Until the D1 database is provisioned (docs/audit-setup.md
      # steps 1-4) the secrets are unset and `wrangler d1 execute --remote`
      # exits 1, which failed this workflow on every nightly run. A
      # job-level `if` cannot read `secrets`/`env`, so the guard is
      # per-step — the same pattern external-link-check.yml already uses.
      - name: Report missing credentials
        if: ${{ env.CLOUDFLARE_API_TOKEN == '' }}
        run: |
          echo "::notice::CLOUDFLARE_API_TOKEN is unset — skipping the D1 export. See docs/audit-setup.md step 4."

      - uses: actions/setup-node@v7
        if: ${{ env.CLOUDFLARE_API_TOKEN != '' }}
        with:
          node-version: 24
          cache: npm

      - run: npm ci
        if: ${{ env.CLOUDFLARE_API_TOKEN != '' }}

      # All three exports in one step so they share the error handling.
      # `wrangler --json` writes its diagnostics to stdout as well as its
      # results, so `> file` swallowed them: a failed run showed nothing but
      # "Process completed with exit code 1", and the actual cause (an
      # invalid database_id, API code 7400) was sitting unread in the output
      # file. Capture both streams and print them on failure.
      - name: Export audit store from D1
        if: ${{ env.CLOUDFLARE_API_TOKEN != '' }}
        run: |
          d1_export() {
            out=$1
            sql=$2
            if ! npx wrangler d1 execute projectcert-audit --remote --json \
                 --command "$sql" > "$out" 2> "$out.err"; then
              echo "::error::D1 export failed: $sql"
              cat "$out.err" || true
              cat "$out" || true
              exit 1
            fi
          }
          d1_export /tmp/verifications.json \
            "SELECT usps, datapoint_id, verified_at, content_hash FROM verifications"
          d1_export /tmp/broken.json \
            "SELECT usps, datapoint_id FROM broken_links"
          d1_export /tmp/accepted-links.json \
            "SELECT url, accepted_status, reviewed_at, note FROM link_reviews WHERE decision = 'accepted'"

      - name: Build ledger and link whitelist
        if: ${{ env.CLOUDFLARE_API_TOKEN != '' }}
        run: |
          npx tsx scripts/build-verification-ledger.ts \
            --verifications /tmp/verifications.json \
            --broken /tmp/broken.json
          npx tsx scripts/build-link-whitelist.ts --accepted /tmp/accepted-links.json

      - name: Open PR if anything changed
        if: ${{ env.CLOUDFLARE_API_TOKEN != '' }}
        uses: peter-evans/create-pull-request@v8
        with:
          branch: chore/audit-store-sync
          base: main
          commit-message: "Sync verification ledger and link whitelist from audit store"
          title: "Sync verification ledger and link whitelist from audit store"
          body: |
            Automated nightly export from the review console's data store:

            - `src/data/verification-ledger.json` — public "datapoints
              reviewed" badges.
            - `src/data/link-whitelist.json` — bot-blocked URLs a reviewer
              accepted as live, which the link checker then trusts.

            This does not modify any state record or `verificationStatus`.
          add-paths: |
            src/data/verification-ledger.json
            src/data/link-whitelist.json
          delete-branch: true
