{
  "extends": "../tsconfig.json",
  "compilerOptions": {
    "types": ["@cloudflare/workers-types"],
    "module": "esnext",
    "target": "esnext",
    "moduleResolution": "bundler",
    "noEmit": true
  },
  "include": ["**/*.ts"],
  "//": "Override the root exclude, which lists 'functions' (and would exclude this dir itself). Imported src/lib files are pulled in via import resolution.",
  "exclude": []
}
