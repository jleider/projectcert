{
  "extends": "astro/tsconfigs/strict",
  "compilerOptions": {
    "baseUrl": ".",
    "paths": {
      "@/*": ["src/*"]
    },
    "types": ["astro/client", "@types/node"],
    "noUncheckedIndexedAccess": true,
    "noImplicitOverride": true,
    "noFallthroughCasesInSwitch": true
  },
  "include": [".astro/types.d.ts", "**/*"],
  "comment:exclude": "functions/ is typechecked by functions/tsconfig.json under @cloudflare/workers-types. The API integration test imports those modules, so it must be excluded here too — otherwise import resolution pulls Workers-typed code into this DOM/Node program. It is typechecked at runtime by Vitest and linted by ESLint. eslint.config.js is excluded because checkJs flags the @deprecated JSDoc on typescript-eslint's config() helper (ts6387); the file is linted on every ESLint run and never shipped.",
  "exclude": [
    "dist",
    "node_modules",
    "functions",
    "tests/audit-api.integration.test.ts",
    "eslint.config.js"
  ]
}
