{
  "extends": "astro/tsconfigs/strict",
  "compilerOptions": {
    "paths": {
      "@/*": ["./src/*"]
    },
    "types": ["astro/client", "@types/node"],
    "noUncheckedIndexedAccess": true,
    "noImplicitOverride": true,
    "noFallthroughCasesInSwitch": true
  },
  "include": [".astro/types.d.ts", "**/*"],
  "comment:exclude": "functions/ is typechecked by functions/tsconfig.json under @cloudflare/workers-types. The API integration test imports those modules, so it is excluded here (it is type-checked by tsconfig.tests.json instead) — otherwise import resolution pulls Workers-typed code into this DOM/Node program. tests/e2e/**/*.mjs is a standalone node-run harness. eslint.config.js is excluded because checkJs flags the @deprecated JSDoc on typescript-eslint's config() helper (ts6387); it is linted on every ESLint run and never shipped.",
  "exclude": [
    "dist",
    "node_modules",
    "functions",
    "tests/audit-api.integration.test.ts",
    "tests/e2e/**/*.mjs",
    "eslint.config.js"
  ]
}
